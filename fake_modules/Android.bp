cc_library {
    name: "libdisplayconfig.qti",
    srcs: ["fake_lib.c"],
    vendor_available: true,
    enabled: select(soong_config_variable("cgsi", "enabled"), {
        true: true,
        default: false,
    }),
}

cc_library {
    name: "libdisplaydebug",
    srcs: ["fake_lib.c"],
    vendor_available: true,
    enabled: select(soong_config_variable("cgsi", "enabled"), {
        true: true,
        default: false,
    }),
}

cc_library {
    name: "libdrmutils",
    srcs: ["fake_lib.c"],
    vendor_available: true,
    enabled: select(soong_config_variable("cgsi", "enabled"), {
        true: true,
        default: false,
    }),
}

cc_library {
    name: "libgps.utils",
    srcs: ["fake_lib.c"],
    vendor_available: true,
    enabled: select(soong_config_variable("cgsi", "enabled"), {
        true: true,
        default: false,
    }),
}

cc_library {
    name: "libgralloc.qti",
    srcs: ["fake_lib.c"],
    vendor_available: true,
    enabled: select(soong_config_variable("cgsi", "enabled"), {
        true: true,
        default: false,
    }),
}

cc_library {
    name: "libgrallocutils",
    srcs: ["fake_lib.c"],
    vendor_available: true,
    enabled: select(soong_config_variable("cgsi", "enabled"), {
        true: true,
        default: false,
    }),
}

cc_library {
    name: "libloc_core",
    srcs: ["fake_lib.c"],
    vendor_available: true,
    enabled: select(soong_config_variable("cgsi", "enabled"), {
        true: true,
        default: false,
    }),
}

cc_library {
    name: "liblocation_api",
    srcs: ["fake_lib.c"],
    vendor_available: true,
    enabled: select(soong_config_variable("cgsi", "enabled"), {
        true: true,
        default: false,
    }),
}

cc_library {
    name: "libqdMetaData",
    srcs: ["fake_lib.c"],
    vendor_available: true,
    enabled: select(soong_config_variable("cgsi", "enabled"), {
        true: true,
        default: false,
    }),
}

cc_library {
    name: "libqdutils",
    srcs: ["fake_lib.c"],
    vendor_available: true,
    enabled: select(soong_config_variable("cgsi", "enabled"), {
        true: true,
        default: false,
    }),
}

cc_library {
    name: "libqservice",
    srcs: ["fake_lib.c"],
    vendor_available: true,
    enabled: select(soong_config_variable("cgsi", "enabled"), {
        true: true,
        default: false,
    }),
}

cc_library {
    name: "libsdedrm",
    srcs: ["fake_lib.c"],
    vendor_available: true,
    enabled: select(soong_config_variable("cgsi", "enabled"), {
        true: true,
        default: false,
    }),
}

cc_library {
    name: "libsdmutils",
    srcs: ["fake_lib.c"],
    vendor_available: true,
    enabled: select(soong_config_variable("cgsi", "enabled"), {
        true: true,
        default: false,
    }),
}

cc_library_headers {
    name: "qti_kernel_headers",
    export_include_dirs: ["."],
    recovery_available: true,
    vendor_available: true,
    enabled: select(soong_config_variable("cgsi", "enabled"), {
        true: true,
        default: false,
    }),
}
